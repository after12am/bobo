#!/usr/bin/php
<?
require_once('../lib/bootstrap.php');

$option = $argv[1];

if (is_file("../lib/commands/$option")) {
    require_once("../lib/commands/$option");
    exit;
}

require_once("../lib/commands/help");
exit;